# 运算符

```python
** 指数（最高优先级）
按位翻转和非 ^ !
乘，除，取模和取除 -  / % //+ -
加法减法 + -
右移左移 >> <<
判断运算符 == >= <= > < !=
赋值运算符 = += -= *= /= %= **= //=
身份运算符 is not  is 判断内存地址是否相同
成员运算符 in not in  判断成员是否存在
逻辑运算符 and or not 与 或 非
```
